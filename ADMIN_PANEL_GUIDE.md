# Руководство по использованию админ-панели

## Доступ к админ-панели

1. Откройте `admin.html` в браузере
2. Введите пароль (по умолчанию: `admin2025`)
3. Пароль можно изменить в файле `.env` (переменная `ADMIN_PASSWORD`)

## Основные функции

### 1. 📊 Дашборд

Показывает:
- Общее количество товаров
- Количество заказов (всего, ожидающих, оплаченных)
- Общую выручку
- Товары с низким остатком на складе

### 2. 📦 Управление товарами

#### Добавление товара
1. Нажмите кнопку **"➕ Добавить товар"**
2. Заполните форму:
   - **Название товара** (обязательно)
   - **Категория** (например: iPhone, iPad, MacBook)
   - **Цена** (₽)
   - **Количество на складе**
   - **Описание**
   - **Изображение** (URL или загрузка файла)
   - **Дополнительные изображения** (загрузка файлов)
   - **Активный товар** (чекбокс)
3. Нажмите **"Сохранить"**

#### Редактирование товара
1. В списке товаров нажмите кнопку **✏️** рядом с товаром
2. Измените нужные поля
3. Нажмите **"Сохранить"**

#### Удаление/деактивация товара
1. В списке товаров нажмите кнопку **🗑️** рядом с товаром
2. Подтвердите действие
3. Товар будет деактивирован (не удален из базы)

#### Обновление количества на складе
1. В списке товаров измените значение в поле **Stock**
2. Изменения сохраняются автоматически при потере фокуса

### 3. ⚡ Массовые операции

#### Изменение цен на процент

1. Перейдите в раздел **"⚡ Массовые операции"**
2. В разделе **"💰 Изменение цен на процент"**:
   - Выберите категорию (или оставьте "Все категории")
   - Введите процент изменения (например: 10 для 10%)
   - Выберите действие: **Повысить** или **Понизить** цены
3. Нажмите **"Применить изменение цен"**
4. Подтвердите действие

**Пример:** Повышение цен на 10% для всех iPhone:
- Выберите категорию "iPhone"
- Введите 10
- Выберите "Повысить цены"
- Нажмите "Применить"

#### Импорт товаров из Excel/CSV

1. Перейдите в раздел **"⚡ Массовые операции"**
2. В разделе **"📥 Импорт товаров из Excel/CSV"**:
   - Нажмите **"Выберите файл"**
   - Выберите файл `.xlsx`, `.xls` или `.csv`
3. Нажмите **"Импортировать товары"**
4. Подтвердите действие

**Формат файла Excel/CSV:**

Файл должен содержать следующие колонки:
- `name` или `Название` или `Name` - название товара (обязательно)
- `category` или `Категория` или `Category` - категория товара
- `price` или `Цена` или `Price` - цена товара (обязательно)
- `stock` или `Количество` или `Stock` - количество на складе
- `description` или `Описание` или `Description` - описание товара
- `image` или `Изображение` или `Image` - URL изображения
- `images` - дополнительные изображения (через запятую)
- `active` или `Активный` - активен ли товар (Да/Нет)

**Пример Excel файла:**

| name | category | price | stock | description |
|------|----------|-------|-------|-------------|
| iPhone 15 Pro | iPhone | 89990 | 10 | Новый iPhone 15 Pro |
| iPad Air | iPad | 59990 | 5 | iPad Air с M2 |

**Важно:**
- Если товар с таким названием уже существует, он будет обновлен
- Если товар не существует, он будет создан
- Обязательные поля: `name` и `price`
- Остальные поля опциональны

### 4. 🛒 Управление заказами

#### Просмотр заказов
1. Перейдите в раздел **"🛒 Заказы"**
2. Используйте фильтр по статусу для фильтрации заказов

#### Изменение статуса заказа
1. В списке заказов выберите новый статус из выпадающего списка
2. Статус обновится автоматически

**Статусы заказов:**
- **Ожидает** (pending) - заказ создан, но еще не обработан
- **Зарезервирован** (reserved) - товары зарезервированы для заказа
- **Оплачен** (paid) - заказ оплачен
- **В обработке** (processing) - заказ обрабатывается
- **Отправлен** (shipped) - заказ отправлен покупателю
- **Доставлен** (delivered) - заказ доставлен
- **Отменен** (cancelled) - заказ отменен

## Настройка API

Админ-панель использует API по адресу: `http://localhost:3000/api/admin`

Для работы админ-панели необходимо:
1. Запустить сервер: `npm start`
2. Убедиться, что MongoDB запущен и подключен
3. Проверить, что файл `.env` настроен правильно

## Безопасность

⚠️ **Важно для продакшена:**
- Измените пароль администратора в `.env` файле
- Используйте HTTPS для защиты соединения
- Ограничьте доступ к админ-панели по IP (при необходимости)
- Настройте CORS для защиты от несанкционированного доступа

## Поддержка

Если возникли проблемы:
1. Проверьте консоль браузера (F12) на наличие ошибок
2. Проверьте логи сервера
3. Убедитесь, что MongoDB запущен и подключен
4. Проверьте файл `.env` на правильность настроек

## Подготовка к следующему этапу

✅ Админ-панель готова для:
- Добавления и редактирования товаров
- Управления складом (stock)
- Массовых операций с ценами
- Импорта товаров из Excel/CSV

✅ API готово для:
- Создания заказов (`/api/orders/reserve`)
- Подтверждения заказов после оплаты (`/api/orders/confirm`)
- Отмены заказов (`/api/orders/cancel`)
- Автоматического резервирования товаров
- Автоматического списания товаров при оплате
- Защиты от перепродажи (двойного заказа одного товара)

**Следующий этап:** Интеграция с платежными системами и обновление фронтенда для работы с API.

