# 🚀 Система управления товарами и заказами

Полноценная система для интернет-магазина с базой данных, админ-панелью и системой заказов.

## ✨ Возможности

- ✅ **База данных MongoDB** - хранение товаров, заказов, резервирований
- ✅ **Автоматическое обновление stock** - при покупке товар сразу списывается
- ✅ **Защита от двойных покупок** - транзакции и резервирование товаров
- ✅ **Админ-панель** - управление товарами, заказами, stock
- ✅ **WebSocket** - обновления stock в реальном времени
- ✅ **Система резервирования** - товары резервируются на 15 минут при оформлении заказа
- ✅ **API для frontend** - все данные загружаются с сервера

## 📦 Установка

### 1. Установите зависимости

```bash
npm install
```

### 2. Настройте переменные окружения

Создайте файл `.env`:

```env
NODE_ENV=development
PORT=3000
MONGODB_URI=mongodb://localhost:27017/apple-favorite
JWT_SECRET=your-secret-key
ADMIN_PASSWORD=admin2025
CORS_ORIGIN=http://localhost:3000
```

### 3. Запустите MongoDB

**Локально:**
```bash
# Установите MongoDB и запустите
mongod
```

**Или используйте MongoDB Atlas (облачная БД):**
- Создайте аккаунт на https://www.mongodb.com/cloud/atlas
- Создайте кластер
- Скопируйте connection string в `.env`

### 4. Мигрируйте товары в базу данных

```bash
node server/scripts/migrate-products.js
```

Этот скрипт перенесет все товары из `script.js` в MongoDB.

### 5. Запустите сервер

```bash
npm start
```

Или для разработки с автоперезагрузкой:

```bash
npm run dev
```

## 🎯 Использование

### Frontend

Откройте `http://localhost:3000` - сайт будет работать с данными из базы.

### Админ-панель

Откройте `http://localhost:3000/admin.html` и войдите с паролем из `.env`.

**Возможности админ-панели:**
- Просмотр статистики (товары, заказы, выручка)
- Управление товарами (добавление, редактирование, удаление)
- Управление stock товаров
- Просмотр и управление заказами
- Изменение статусов заказов

### API Endpoints

**Товары:**
- `GET /api/products` - получить все товары
- `GET /api/products/:id` - получить товар по ID
- `POST /api/products/:id/check` - проверить доступность товара

**Заказы:**
- `POST /api/orders/reserve` - зарезервировать товары
- `POST /api/orders/confirm` - подтвердить заказ (после оплаты)
- `POST /api/orders/cancel` - отменить заказ
- `GET /api/orders/:orderId` - получить заказ

**Админ (требует авторизации):**
- `GET /api/admin/products` - получить все товары
- `PUT /api/admin/products/:id` - обновить товар
- `PUT /api/admin/products/:id/stock` - обновить stock
- `GET /api/admin/orders` - получить все заказы
- `GET /api/admin/stats` - получить статистику

## 🔄 Как это работает

### Процесс покупки:

1. **Пользователь добавляет товар в корзину**
   - Frontend проверяет доступность через API

2. **При переходе к оплате:**
   - Создается резервирование товара на 15 минут
   - Stock уменьшается на количество зарезервированного товара
   - Другие пользователи видят обновленный stock

3. **При успешной оплате:**
   - Резервирование подтверждается
   - Товар списывается из stock окончательно
   - Создается заказ со статусом "paid"

4. **При ошибке/отмене оплаты:**
   - Резервирование отменяется
   - Stock возвращается обратно

5. **Автоматическая очистка:**
   - Каждую минуту cron job проверяет истекшие резервирования
   - Автоматически возвращает товары в stock

### Защита от двойных покупок:

- Используются MongoDB транзакции
- Резервирование товаров перед оплатой
- Оптимистичная блокировка через поле `version`
- WebSocket уведомления для всех клиентов

## 📁 Структура проекта

```
project/
├── server.js                    # Главный файл сервера
├── config/
│   └── config.js               # Конфигурация
├── server/
│   ├── models/                 # Модели MongoDB
│   │   ├── Product.js
│   │   ├── Order.js
│   │   └── Reservation.js
│   ├── routes/                 # API routes
│   │   ├── products.js
│   │   ├── orders.js
│   │   └── admin.js
│   └── scripts/               # Скрипты
│       └── migrate-products.js
├── admin.html                  # Админ-панель
├── index.html                  # Главная страница
└── ...
```

## 🚀 Деплой на хост

См. подробную инструкцию в `DEPLOY_INSTRUCTIONS.md`

## 🔒 Безопасность

- ✅ Переменные окружения для секретов
- ✅ Авторизация для админ-панели
- ✅ Валидация данных
- ✅ Защита от SQL injection (MongoDB)
- ✅ CORS настройки

## 📝 Примечания

- Stock товаров устанавливается в 10 по умолчанию при миграции
- После миграции обновите stock через админ-панель
- Резервирование действует 15 минут
- WebSocket требует подключения к серверу

## 🐛 Решение проблем

**MongoDB не подключается:**
- Проверьте, что MongoDB запущена
- Проверьте connection string в `.env`
- Проверьте firewall настройки

**Товары не загружаются:**
- Убедитесь, что миграция выполнена
- Проверьте подключение к MongoDB
- Проверьте логи сервера

**Админ-панель не работает:**
- Проверьте пароль в `.env`
- Проверьте CORS настройки
- Откройте консоль браузера для ошибок

