# 🚀 Быстрый старт TechStore

## Открыть сайт

Просто откройте файл `index.html` в любом современном браузере (Chrome, Firefox, Edge, Safari).

Двойной клик по `index.html` → сайт откроется!

## Что работает из коробки

✅ **Товары** - 16 готовых товаров с описаниями и характеристиками  
✅ **Фотогалерея** - Клик по товару → смотрим все фото  
✅ **Корзина** - Добавляем товары, сохраняется автоматически  
✅ **Оформление заказа** - Форма с валидацией  
✅ **Адаптивный дизайн** - Отлично на телефоне и компьютере  

## Добавить 3D модели

1. Скачайте FBX модель (например с Sketchfab)
2. Положите файл в папку `models/`
3. Обновите путь в `script.js`:

```javascript
{
    id: 1,
    name: "MacBook Pro 16\"",
    has3D: true,
    model3D: "models/ваша_модель.fbx"  // ← Тут
}
```

## Добавить свой товар

В файле `script.js` найдите массив `products`:

```javascript
const products = [
    {
        id: 7,                              // Новый ID
        name: "Ваш товар",
        description: "Описание товара",
        price: 50000,                       // Цена в рублях
        image: "url_на_фото",
        images: ["url1", "url2", "url3"],   // Все фото
        specs: ["Характеристика 1", ...],   // Список
        has3D: false,                       // Есть 3D или нет
        model3D: "models/model.fbx"         // Путь к 3D
    }
];
```

## Изменить дизайн

Все стили в файле `styles.css`:

- **Цвета градиентов**: строка 9-11
- **Размеры**: в переменных и классах
- **Анимации**: секция "@keyframes"

## Нужна помощь?

- Полная документация → `README.md`
- Про 3D модели → `models/README.md`

---

**Готово! Ваш магазин работает!** 🎉

