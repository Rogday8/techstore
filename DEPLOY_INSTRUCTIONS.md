# Инструкция по деплою на хост

## 📋 Подготовка к деплою

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка переменных окружения

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите:
- `MONGODB_URI` - URI вашей MongoDB базы данных
- `JWT_SECRET` - секретный ключ для JWT (используйте надежный случайный ключ)
- `ADMIN_PASSWORD` - пароль для админ-панели
- `CORS_ORIGIN` - домен вашего сайта (например: `https://yourdomain.com`)
- `PORT` - порт сервера (обычно 3000 или тот, который предоставляет хост)

### 3. Миграция товаров в базу данных

Перед первым запуском необходимо перенести товары из `script.js` в MongoDB:

```bash
node server/scripts/migrate-products.js
```

Этот скрипт:
- Прочитает товары из `script.js`
- Преобразует их в формат базы данных
- Сохранит в MongoDB
- Установит stock = 10 для всех товаров по умолчанию

**Важно:** После миграции вы можете изменить stock через админ-панель.

---

## 🚀 Деплой на хост

### Вариант 1: VPS/Выделенный сервер

#### Требования:
- Node.js 16+ и npm
- MongoDB (локально или MongoDB Atlas)
- PM2 для управления процессом (рекомендуется)

#### Шаги:

1. **Загрузите файлы на сервер:**
   ```bash
   # Используйте scp, rsync или git clone
   git clone https://github.com/your-repo/apple-favorite.git
   cd apple-favorite
   ```

2. **Установите зависимости:**
   ```bash
   npm install --production
   ```

3. **Настройте `.env` файл:**
   ```bash
   nano .env
   # Укажите все необходимые переменные
   ```

4. **Запустите миграцию:**
   ```bash
   node server/scripts/migrate-products.js
   ```

5. **Установите PM2 (если еще не установлен):**
   ```bash
   npm install -g pm2
   ```

6. **Запустите сервер через PM2:**
   ```bash
   pm2 start server.js --name apple-favorite
   pm2 save
   pm2 startup
   ```

7. **Настройте Nginx (опционально, для проксирования):**
   ```nginx
   server {
       listen 80;
       server_name yourdomain.com;
       
       location / {
           proxy_pass http://localhost:3000;
           proxy_http_version 1.1;
           proxy_set_header Upgrade $http_upgrade;
           proxy_set_header Connection 'upgrade';
           proxy_set_header Host $host;
           proxy_cache_bypass $http_upgrade;
       }
   }
   ```

### Вариант 2: MongoDB Atlas (облачная база данных)

1. **Создайте аккаунт на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)**

2. **Создайте кластер** (бесплатный tier доступен)

3. **Получите connection string:**
   - Нажмите "Connect" на вашем кластере
   - Выберите "Connect your application"
   - Скопируйте connection string
   - Замените `<password>` на ваш пароль

4. **Добавьте IP адрес вашего сервера в whitelist**

5. **Укажите connection string в `.env`:**
   ```
   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/apple-favorite
   ```

### Вариант 3: Heroku / Railway / Render

#### Heroku:

1. **Установите Heroku CLI**

2. **Создайте приложение:**
   ```bash
   heroku create your-app-name
   ```

3. **Добавьте MongoDB Atlas addon:**
   ```bash
   heroku addons:create mongolab:sandbox
   ```

4. **Настройте переменные окружения:**
   ```bash
   heroku config:set ADMIN_PASSWORD=your-password
   heroku config:set JWT_SECRET=your-secret
   heroku config:set NODE_ENV=production
   ```

5. **Деплой:**
   ```bash
   git push heroku main
   ```

6. **Запустите миграцию:**
   ```bash
   heroku run node server/scripts/migrate-products.js
   ```

#### Railway / Render:

1. Подключите ваш GitHub репозиторий
2. Укажите переменные окружения в настройках
3. Укажите команду запуска: `node server.js`
4. После деплоя запустите миграцию через консоль

---

## 🔧 Настройка после деплоя

### 1. Проверка работы API

Откройте в браузере:
- `https://yourdomain.com/api/products` - должен вернуть список товаров
- `https://yourdomain.com/api/admin/stats` - должен вернуть ошибку авторизации (это нормально)

### 2. Доступ к админ-панели

Откройте `https://yourdomain.com/admin.html` и войдите с паролем из `.env`

### 3. Настройка stock товаров

1. Войдите в админ-панель
2. Перейдите в раздел "Товары"
3. Обновите stock для каждого товара

---

## 📁 Структура файлов на хосте

```
/
├── server.js                 # Главный файл сервера
├── package.json              # Зависимости
├── .env                      # Переменные окружения (НЕ коммитить в git!)
├── config/
│   └── config.js            # Конфигурация
├── server/
│   ├── models/              # Модели MongoDB
│   ├── routes/              # API routes
│   └── scripts/             # Скрипты миграции
├── images/                  # Изображения товаров
├── index.html               # Главная страница
├── admin.html               # Админ-панель
└── ...                      # Остальные файлы
```

---

## 🔒 Безопасность

### Обязательно:

1. **Никогда не коммитьте `.env` в git!**
   - Убедитесь, что `.env` в `.gitignore`

2. **Используйте HTTPS** для продакшена

3. **Измените пароль админа** на надежный

4. **Используйте надежный JWT_SECRET** (минимум 32 символа, случайные)

5. **Ограничьте доступ к MongoDB** (whitelist IP адресов)

6. **Регулярно обновляйте зависимости:**
   ```bash
   npm audit
   npm update
   ```

---

## 🐛 Решение проблем

### Сервер не запускается

1. Проверьте, что MongoDB доступна:
   ```bash
   # Проверьте connection string в .env
   # Проверьте, что MongoDB запущена
   ```

2. Проверьте логи:
   ```bash
   pm2 logs apple-favorite
   # или
   node server.js
   ```

### Товары не загружаются

1. Проверьте, что миграция выполнена:
   ```bash
   node server/scripts/migrate-products.js
   ```

2. Проверьте подключение к MongoDB:
   ```bash
   # В MongoDB Compass или через mongo shell
   use apple-favorite
   db.products.find().count()
   ```

### Админ-панель не работает

1. Проверьте, что пароль в `.env` совпадает с тем, что вы вводите
2. Проверьте CORS настройки в `config/config.js`
3. Проверьте консоль браузера на ошибки

---

## 📞 Поддержка

Если возникли проблемы:
1. Проверьте логи сервера
2. Проверьте логи MongoDB
3. Убедитесь, что все переменные окружения установлены правильно
4. Проверьте, что порты открыты в firewall

---

## ✅ Чеклист перед запуском

- [ ] MongoDB настроена и доступна
- [ ] `.env` файл создан и заполнен
- [ ] Миграция товаров выполнена
- [ ] Зависимости установлены (`npm install`)
- [ ] Сервер запускается без ошибок
- [ ] API доступен (`/api/products`)
- [ ] Админ-панель доступна (`/admin.html`)
- [ ] Stock товаров настроен
- [ ] HTTPS настроен (для продакшена)
- [ ] Firewall настроен правильно

