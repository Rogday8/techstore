# 🚀 Быстрый деплой на GitHub Pages

## 📋 Пошаговая инструкция

### ШАГ 1: Создайте репозиторий на GitHub

1. Откройте: **https://github.com/new**
2. Заполните:
   - **Repository name:** `techstore` (или любое название)
   - **Description:** `TechStore - Интернет-магазин премиальной техники`
   - **Public** ✓ (ОБЯЗАТЕЛЬНО для бесплатного GitHub Pages!)
   - ❌ **НЕ СТАВЬТЕ** галочки на README, .gitignore, license
3. Нажмите **"Create repository"**

---

### ШАГ 2: Настройте удаленный репозиторий

Выполните в PowerShell (в папке проекта):

```powershell
git remote add origin https://github.com/ВАШ_НИК/techstore.git
```

⚠️ **Замените `ВАШ_НИК` на ваш GitHub username!**

Если remote уже существует, сначала удалите его:
```powershell
git remote remove origin
git remote add origin https://github.com/ВАШ_НИК/techstore.git
```

---

### ШАГ 3: Отправьте файлы на GitHub

**Вариант А: Автоматический (рекомендуется)**

Запустите скрипт:
```powershell
.\DEPLOY_GITHUB.ps1
```

**Вариант Б: Вручную**

```powershell
# Добавить все файлы
git add .

# Создать коммит
git commit -m "Deploy to GitHub Pages"

# Отправить на GitHub
git push -u origin main
```

⚠️ **При запросе аутентификации:**
- Логин: ваш GitHub username
- Пароль: используйте **Personal Access Token** (не пароль от аккаунта!)
- Как создать токен: https://docs.github.com/en/authentication/keeping-your-account-and-data-secure/creating-a-personal-access-token

---

### ШАГ 4: Включите GitHub Pages

1. Откройте ваш репозиторий на GitHub
2. Нажмите **"Settings"** (вкладка сверху)
3. В левом меню выберите **"Pages"**
4. В разделе **"Source"**:
   - Выберите **"Deploy from a branch"**
   - **Branch:** `main`
   - **Folder:** `/ (root)`
5. Нажмите **"Save"**

---

### 🎉 ГОТОВО!

Через **1-2 минуты** ваш сайт будет доступен:

```
https://ВАШ_НИК.github.io/techstore/
```

---

## 🔄 Обновление сайта

После любых изменений:

```powershell
git add .
git commit -m "Update site"
git push
```

Сайт обновится автоматически через 1-2 минуты!

---

## 🆘 Решение проблем

### ❌ "remote origin already exists"
```powershell
git remote remove origin
git remote add origin https://github.com/ВАШ_НИК/techstore.git
```

### ❌ Ошибка при push
- Проверьте правильность URL репозитория
- Используйте Personal Access Token вместо пароля
- Убедитесь что репозиторий существует на GitHub

### ❌ Не видно кнопку Pages в Settings
- Убедитесь что репозиторий **Public**
- Подождите 1-2 минуты после создания

### ❌ Сайт не открывается
- Подождите 2-3 минуты (GitHub Pages может занять время)
- Очистите кэш браузера (Ctrl+F5)
- Проверьте что branch = `main` и folder = `/ (root)`

---

## 📁 Какие файлы загружать

**Обязательно:**
- ✅ index.html
- ✅ styles.css
- ✅ script.js
- ✅ images/ (вся папка)
- ✅ models/ (вся папка)

**Можно (опционально):**
- 📄 README.md
- 📄 Другие .md файлы
- 📄 Серверные файлы (server.py, .bat файлы)

**Не нужно:**
- ❌ .git/ (создается автоматически)
- ❌ node_modules/ (если будете добавлять)

---

## ✅ Что будет работать на GitHub Pages

- ✅ Весь интерфейс и стили
- ✅ Каталог товаров
- ✅ Корзина (localStorage)
- ✅ Форма заказа (отправка требует backend)
- ✅ Модальные окна
- ✅ Фотогалерея
- ✅ Фильтры по категориям
- ✅ 3D модели через model-viewer

---

**Удачи! 🚀**
