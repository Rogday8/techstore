# ⚡ Быстрый старт

## 1. Установка зависимостей

```bash
npm install
```

## 2. Настройка MongoDB

**Вариант A: Локальная MongoDB**
- Установите MongoDB
- Запустите: `mongod`

**Вариант B: MongoDB Atlas (рекомендуется)**
- Создайте аккаунт на https://www.mongodb.com/cloud/atlas
- Создайте бесплатный кластер
- Скопируйте connection string

## 3. Создайте `.env` файл

```bash
cp .env.example .env
```

Отредактируйте `.env`:
```env
MONGODB_URI=mongodb://localhost:27017/apple-favorite
# или для Atlas:
# MONGODB_URI=mongodb+srv://user:pass@cluster.mongodb.net/apple-favorite

ADMIN_PASSWORD=admin2025
JWT_SECRET=your-secret-key-here
PORT=3000
NODE_ENV=development
```

## 4. Миграция товаров

```bash
node server/scripts/migrate-products.js
```

## 5. Запуск сервера

```bash
npm start
```

## 6. Откройте в браузере

- **Сайт:** http://localhost:3000
- **Админ-панель:** http://localhost:3000/admin.html (пароль: `admin2025`)

## ✅ Готово!

Теперь ваш сайт работает с базой данных. Все товары загружаются с сервера, stock обновляется автоматически.

---

## 📝 Что дальше?

1. **Настройте stock товаров** через админ-панель
2. **Протестируйте покупку** - товар должен резервироваться и списываться
3. **Подготовьте к деплою** - см. `DEPLOY_INSTRUCTIONS.md`

